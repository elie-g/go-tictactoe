package board

import (
    "github.com/hajimehoshi/ebiten"
)
var gridImage *ebiten.Image

func (g *board) DrawBoard(screen *ebiten.Image) {
    g.screen = screen
    
    if gridImage == nil {
    
    }
}